import 'package:flutter/cupertino.dart';

class ProfelView extends StatelessWidget {
  const ProfelView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text("ProfelView"),
    );
  }
}
